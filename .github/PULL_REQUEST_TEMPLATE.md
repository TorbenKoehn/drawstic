<!-- PR title must follow Conventional Commits, e.g. `feat(parser): support ...` -->

## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] `bun run format` and `bun run test` pass
- [ ] For `src/` changes: `bun run typecheck` and `bun run build` pass
- [ ] Material decision? Added an ADR under `docs/decisions/` and synced the index
